@Library('dst-shared@master') _

dockerBuildPipeline {
    app = "tftpd"
    name = "TFTPD"
    description = "Cray Management System TFTPD service"
    repository = "cray"
    imagePrefix = "cray"
    product = "csm"
}