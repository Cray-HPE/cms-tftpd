@Library('dst-shared@release/shasta-1.4') _

dockerBuildPipeline {
    app = "tftpd"
    name = "TFTPD"
    description = "Cray Management System TFTPD service"
    repository = "cray"
    imagePrefix = "cray"
    product = "csm"
}
